import 'package:freezed_annotation/freezed_annotation.dart';

part '{{feature_name}}_model.freezed.dart';
part '{{feature_name}}_model.g.dart';

/// {{FeatureName}} 数据模型。
///
/// {{FeatureName}} data model.
@freezed
abstract class {{FeatureName}}Model with _${{FeatureName}}Model {
  /// 创建模型。
  ///
  /// Creates the model.
  const factory {{FeatureName}}Model() = _{{FeatureName}}Model;

  /// 从 JSON 反序列化。
  ///
  /// Deserializes from JSON.
  factory {{FeatureName}}Model.fromJson(Map<String, dynamic> json) =>
      _${{FeatureName}}ModelFromJson(json);
}
